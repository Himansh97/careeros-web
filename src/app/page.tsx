import { LandingPage } from "@/components/landing/landing-page";

/**
 * The entrance.
 *
 * Was the pre-flight poll, which meant the product had no front door at all —
 * it opened mid-sentence, on a check. The poll moved to `/launch` and this is
 * what precedes it.
 */
export default function RootPage() {
  return <LandingPage />;
}
