# Copy to .env.local (gitignored) to customize locally.

# No backend exists yet. Set to "true" to develop/demo against the
# realistic mock data layer in src/lib/mock/ instead of the honest
# "not connected" empty states.
NEXT_PUBLIC_USE_MOCK_DATA=false
